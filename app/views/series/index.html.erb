<h1>Listing Series</h1>
 
<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
 
  <% @series.each do |series| %>
    <tr>
      <td><%= series.name %></td>
      <td><%= link_to 'Show', series_path(series) %></td>
    </tr>
  <% end %>
</table>